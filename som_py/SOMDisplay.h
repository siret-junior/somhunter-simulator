#include <cstddef>

void som_display(const float *const points,
                 const float *const scores,
                 const size_t n,
                 const size_t dim,
                 const size_t swidth,
                 const size_t sheight,
                 size_t *output);
